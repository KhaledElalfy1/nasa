import 'package:flutter/material.dart';

class SoilMoistureView extends StatelessWidget {
  const SoilMoistureView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Soil Moisture View'),
    );
  }
}